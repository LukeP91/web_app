= simple_form_for [:admin, interest], html: { class: 'form-horizontal' } do |f|
  = f.input :name
  = f.error_span :name
  = f.association :category, include_blank: false
  = f.error_span :category
  = f.button :submit, class: 'btn-primary'
